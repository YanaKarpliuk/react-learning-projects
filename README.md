# React learning projects

A collection of small React apps I built while learning and practicing React.
Each project covers different concepts, from components and props to hooks, routing, and state management.

## How to run locally
`nvm use` - set the correct Node.js version <br>
`npm install` - install dependencies<br>
`npm run dev` - start the development server
